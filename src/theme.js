// src/theme.js
import { createTheme } from '@mui/material/styles';

// Create a custom theme
const theme = createTheme({
  typography: {
    fontFamily: 'Quicksand, Arial, sans-serif', // Set Quicksand as the default font
  },
});

export default theme;
